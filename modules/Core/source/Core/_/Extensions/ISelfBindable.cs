namespace Core
{
    /// <summary>
    /// Interface de marcação para fazer bind das demais interfaces na mesma classe para a classe que esta interface foi herdada
    /// </summary>
    public interface ISelfBindable
    {
    }
}